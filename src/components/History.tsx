import React from "react";
import { Box } from "@chakra-ui/react";

const History: React.FC = () => {
  return (
    <Box
      pr="2.2rem"
      pl={{ base: "2.2rem", lg: 0 }}
      pt={{ base: 0, lg: "2.2rem" }}
    >
      <Box bgColor="#95cfd9" rounded="2.2rem" h="full"></Box>
    </Box>
  );
};

export default History;
